SELECT CAST(AVG(color) AS NUMERIC(6, 2)) FROM 
(SELECT
(
CASE
WHEN Q_ID IN (SELECT B_Q_ID FROM utB) THEN (SELECT CAST(SUM(B_VOL) AS NUMERIC(6, 2)) FROM utB WHERE B_Q_ID=Q_ID)
ELSE CAST(0 AS NUMERIC(6, 2))
END
) AS color
FROM utQ) as t